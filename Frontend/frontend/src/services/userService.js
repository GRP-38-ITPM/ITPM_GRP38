/*import axios from 'axios';

//It get the API model value which has assign in model folder
const USER_API_BASE_URL="http://localhost:8083/api/ITPM/users";

class userService{
    getUsers(){
        return axios.get(USER_API_BASE_URL);
    }
}

// export the get backend datas
export default new userService(); */